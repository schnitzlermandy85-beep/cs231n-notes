---
tags:
  - cs231n
  - 计算机视觉
  - 图像分类
  - 线性分类器
  - kNN
---

# 线性分类器与视觉分类

## 1. 图像分类问题

图像分类的核心任务：预定义一些标签，分类就是把这些标签分给图片使其对应起来。

### 计算机如何理解图像？

计算机将图像视为**数字矩阵**。

### 分类的挑战

不同的角度、背景、光线、类内变异、上下文的影响，都是影响分类的因素。

## 2. 数据驱动方法

机器学习的数据驱动方法分为三步：

1. **收集数据和标签**；
2. **使用机器学习算法训练分类器**；
3. **在新的数据集上评估**。

## 3. k 近邻算法（k-NN）

### 最近邻分类器

- 记住所有的数据；
- 构建预测函数，创建所有的图标和标签；
- 试图找到最相似的图像输出；
- 创建一个距离函数来说明不同函数的距离差异（相似刻画）。

**L1 距离（曼哈顿距离）**：所有数值相减绝对值的和。

曼哈顿距离会线性累加大差值，对异常值相对不敏感；欧氏距离会放大大差值，对异常值更加敏感。

### k 近邻算法流程

看一个新样本周围距离最近的 K 个已知样本，根据这些邻居来判断它属于哪一类。

步骤：
1. 先存下所有的样本；
2. 输入测试样例，计算它和所有训练样本之间的距离；
3. 选择最近的 K 个样本；
4. 根据邻居做出预测。

## 4. 超参数与验证集

### 超参数

超参数是训练前预先设置的参数值。

### 超参数设置的思考

> 是优先考虑训练集还是测试集？答案是**都不是**。

- 如果在训练集考虑 → 过拟合；
- 如果在测试集考虑 → 迎合此测试集，换一个测试集就不行了。

**更好的做法**：不同数据集用不同的超参数，引入**验证集**。

### 交叉验证

将训练集分组，每次只取出部分来实验得到准确率，再取平均值。

## 5. 线性分类器

### 数学原理

$$
f(x, W) = Wx + b
$$

> 但有些实例是不能用线性分类器处理的。

### Softmax 分类器

1. **线性打分层**：由于分数是无界的；
2. **Softmax 归一化**：转化为有界的概率；
3. **损失函数**：Cross-Entropy（交叉熵）。
