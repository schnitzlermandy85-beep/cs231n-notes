---
tags:
  - cs231n
  - 深度学习
  - 正则化
  - 优化算法
---

# 正则化与优化

## 1. 视觉识别的挑战

光线、可变形、闭塞、背景噪声、类变异都会影响检测。

## 2. 损失函数

损失函数用来对比分类器得到的正确值和错误值之间的差距。

常见损失函数：
- **Softmax 损失**
- **交叉熵损失（Cross-Entropy Loss）**

## 3. 正则化

正则化的目的是**在训练集上表现差一点，然后在未发现的数据上表现更好**——即防止过拟合。

### L1 与 L2 正则化

| 正则化 | 形式 | 特点 |
|--------|------|------|
| **L2** | 权重平方矩阵和 | 使权重分散、更平滑 |
| **L1** | 权重模长之和 | 倾向于产生稀疏权重 |

> 正则化为了简化模型，让它在测试集上做得更好，让数据更加分散。

## 4. 优化算法

### 梯度与学习率

- 目标是找到最大的斜率方向。
- H 型数值法可用于估计梯度。

### SGD（随机梯度下降）

- 每次对子集进行采样。
- 缺点：会出现很多杂音影响学习效率。
- 有时会卡在**局部最小值**。

### Momentum（动量）

- 提供更高的动力，避免被控制在局部最优点而不是全局最优点。
- 但动力过大也不利于最后收敛。

### RMSProp

自适应学习率方法，调整每个参数的学习率。

### Adam

结合了 Momentum 和 RMSProp 的优点（笔记中标记为 "A"，待补充）。
