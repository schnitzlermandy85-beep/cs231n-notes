---
tags:
  - CNN
  - 深度学习
  - 计算机视觉
  - 卷积
---

# CNN 卷积神经网络

## 1. 核心思想

卷积把图像的空间结构提前写进了网络结构中。如果使用全连接网络来处理图像，会先将图片变成一维的线性向量，这样做的后果是**空间结构被破坏掉**。

## 2. 卷积操作

### 基本概念

- 卷积的计算可以近似理解成向量的内积：如果模式相似，输出就会变得更大，反之更小。
- 卷积会产生一个**特征图**（feature map）。
- 卷积深度（卷积核的通道数）一定要和图像的输入通道数一致。
- 多个卷积核会产生多个输出通道：比如有六个卷积核，最后的特征图宽度和高度由原图大小和卷积核大小决定（平移），而**卷积核个数**会成为特征图的输出通道数。

### 张量维度（NCHW 格式）

| 维度 | 形状 |
|------|------|
| **输入** | N × 输入通道 × 高 × 宽 |
| **卷积核** | 输出通道 × 输入通道 × 核高 × 核宽 |
| **输出** | N × 输出通道 × 输出高 × 输出宽 |

> 输出高/宽的计算公式：$H_{out} = \frac{H_{in} - K + 2P}{S} + 1$（K = 核大小，P = padding，S = stride）

### Stride 与 Padding

- **stride**：卷积核每次移动的距离。
- **padding**：在输入周围补零，防止多次卷积后特征图过小。

## 3. 关键特性

### 参数共享

同一个卷积核会在不同位置被反复使用——所有位置都会被这个卷积核扫过，因此共用同一个参数，这称为**参数共享**。

> 全连接层和卷积层的参数量是不同的：卷积层通过参数共享大幅减少了参数量。

### 平移等变性

平移等变可以理解成：**特征怎么移动，特征层的对应部分就怎么移动**。即输入平移会导致输出发生相同的平移。

### 层次化特征

- 一个卷积层可以有多个卷积核，每个卷积核学习不同的特征。
- 多层卷积是把卷积层连续叠起来：一层卷积得到一个特征图，这个特征图继续通过下一层卷积，高层卷积可以学习特征之间的关系（即前一层和当前层卷积提取的特征之间的关系）。
- 随着层数的加深，特征会逐渐组合，从低级（边缘、纹理）逐渐抽象为高级（物体部件、完整物体）。

### 感受野

感受野表示特征图中的一个神经元最终能够看到的原始输入图像中的多大区域——即特征图对应上一层的哪个部分。

- 这解释了为什么更高层会看到更大的范围（感受野逐步扩大）。
- 利用更大的感受野，高层特征可以捕捉到整体的关系。

## 4. 池化层

池化层负责**下采样**（降维），最常见的是**最大池化**（Max Pooling）：选择池化窗口中的最大值作为采样结果。

- **前向传播**：取窗口内最大值。
- **反向传播**：将上游梯度传递给前向传播中最大值所在的位置，其余位置梯度为 0。

## 5. 设计技巧

多个小卷积核叠加优于单个大卷积核，因为可以：

1. **增加网络深度**，加入更多非线性激活。
2. **使用更少的参数**（例如两个 3×3 卷积的感受野等于一个 5×5 卷积，但参数量更少）。
3. **逐层构造复杂的特征**。
