---
tags:
  - cs231n
  - 深度学习
  - 神经网络
  - 反向传播
  - 梯度下降
---

# 神经网络与反向传播

## 1. 目标函数与损失函数

- **目标函数**：训练时希望优化的总体目标。
- **损失函数**：衡量模型预测结果与真实标签之间差距的函数。

二者关系：
- 损失函数通常是目标函数的一部分。
- 训练神经网络时，模型会通过最小化损失函数来改进参数。

常见形式：
- **分类任务**：常用交叉熵损失。
- **回归任务**：常用均方误差损失。

> 交叉熵衡量模型给出的概率分布与真实值的差距：$L = -\log p_{\text{正确类别}}$

## 2. Softmax

Softmax 是深度学习分类任务中常用的函数，常用于把模型输出的分数转换成各类别的概率分布。

- Softmax 不是唯一的分类函数。
- 它通常和交叉熵损失一起使用。

## 3. 神经网络的特点

线性分类器无法表示非线性结构的数据，神经网络适合处理**任意**结构的数据。

### 激活函数的作用

**防止退化成线性分类器。**

### ReLU

ReLU 将小于 0 的数舍去，只将大于 0 的数保留下来。

- 如果前向 > 0 → 神经元被激活 → 反向时允许梯度通过。
- 如果前向 ≤ 0 → 梯度被截断。

## 4. 神经网络训练流程

1. 输入数据进入网络。
2. 每一层根据参数计算输出。
3. 最后一层得到预测结果。
4. 使用损失函数计算预测值与真实值的差距。
5. 通过反向传播计算每个参数对损失的影响。
6. 使用优化算法更新参数。

> **总结**：神经网络定义从输入到预测的函数，前向传播计算损失，反向传播计算梯度，SGD/Adam 根据梯度更新参数。

## 5. 反向传播

反向传播的核心作用是**计算梯度**，即判断每个参数应该如何调整，才能让损失函数变小。

> 反向传播不优化梯度，只是负责**计算梯度**。

### 基本思路

- 从输出层开始，根据损失函数计算误差。
- 按照链式法则，把误差一层一层向前传回去。
- 得到每一层参数的梯度。
- 优化器根据梯度更新参数。

### 链式法则

反向传播的核心是利用链式法则：

$$
\text{输入梯度} = \text{上游梯度} \times \text{局部梯度}
$$

输入梯度 = 上游梯度（反向传播得到）× 局部梯度（此处），然后作为下一个节点的上游梯度。

> 前向计算的过程一定要保留好中间必要的**中间变量**。

### 常见计算节点

- **加法节点**
- **乘法节点**
- **ReLU 节点**：截断负数
- **分流节点**：一个变量流向多个分支

### 汇合梯度处理

用多元链式法则处理多条路径影响损失的情况。同一个偏置被 batch 中所有样本共享，因此每个样本都会产生一份对该偏置的梯度。根据多路径梯度相加原则，需要沿 batch 维度将这些贡献求和。

## 6. 梯度与参数更新

梯度表示损失函数对参数变化的敏感程度：

- **梯度方向**表示损失上升最快的方向。
- 参数更新时通常沿着梯度的**反方向**移动，使损失下降。

简单公式：

```
新参数 = 旧参数 - 学习率 × 梯度
```

其中，学习率控制每次参数更新的步长。

> **反向传播 ≠ 梯度下降**：反向传播主要是计算损失对每个参数的敏感程度，梯度下降则是优化算法更新参数的过程。反向传播发生在梯度下降之前，可以理解为"求导"的过程。
